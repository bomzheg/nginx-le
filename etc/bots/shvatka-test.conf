# include this file to nginx.conf in server partition
location /shvatka-test/ {
    resolver 127.0.0.11 valid=30s;
    set $u_shvatka_test shvatka-test;
    proxy_pass http://$u_shvatka_test:80;
    rewrite ^/shvatka-test(.*)$ $1 break;
}
